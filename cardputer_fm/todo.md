file preview for images
text editor (not just append/delete)


horizontal scrolling (real nano feel)
status bar with filename + modified flag
line numbers
search (Ctrl+W)
go to line
🧠 Advanced
undo system
syntax coloring (basic)
soft tabs / indentation

If you want, next step I’d recommend:
👉 horizontal scrolling + status bar (real nano UX)

I can add:
    line numbers
    persistent cursor position when paging
    Ctrl+arrow word jump
    search (Ctrl+W)

---------------------
add a keycode command to type a char/char combination keycode
- exit on double q pressing


https://github.com/RetiredWizard/PyDOS
-Implementing a proper "redraw only changed parts"
